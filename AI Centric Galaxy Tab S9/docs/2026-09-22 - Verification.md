# Galaxy Workspace verification

Last Updated: 2026-09-22

## September 22 standalone release checks

The current deployment runs its Node/SQLite/files and official Codex client inside Termux on the SM-X810. Galaxy Workspace 0.3.0 and Galaxy Device Tools 0.2.0 are installed. The user's independent-tablet requirement supersedes the workstation-only deployment described in the historical records below.

Verified on the actual tablet:

- The user completed the official Codex device sign-in and MFA on the tablet. A direct local model smoke returned the requested marker, then a complete application request assessed the synthetic Meeting kit and saved `Reports/Summary report.md` through the typed document tools. It correctly refused to invent a Capital owner when that row was absent. Actual tool receipts, saved report and final answer were inspected; original sources were preserved.
- Local System identity, Android version, battery, storage, memory and four settings returned actual Android data. Shared storage listed 16 public folders. A screen-timeout proposal was applied, read back, undone, and verified equal to the original value. No setting change was left by that test.
- Both connected OneDrive accounts passed real identity checks and root browsing locally: Personal returned 98 items; the migrated linked Microsoft CLI account returned 8. No cloud file was written by these tests.
- The updated user guide was rendered locally to a 53,573-byte PDF in the tablet’s Downloads folder: `2026-09-22 - Galaxy Workspace User Guide.pdf`.
- The saved synthetic report exported on-device to Word (11,117 bytes, ZIP signature) and PDF (18,107 bytes, PDF signature), using Android-native Pandoc/Typst. The local export pipeline was inspected; Android's existing save picker had been exercised with actual files in the earlier report-workflow checks.
- The native Outlook reader opened today's Agenda and performed a submitted text search across All Accounts without ADB. It returned dated visible data and preserved the `connect@adamgoodwin.ca` sign-in warning. Calendar loading and cached content remain explicitly partial; no current minister meeting was established by this verification.
- Authenticated helper protocol checks rejected an invalid bearer, a browser Origin, arbitrary shell operation, unsupported setting/name/value and stale/send control. A valid identity request still worked afterward. The listener is loopback-only.
- A follow-up to the new on-tablet document conversation resumed its actual Codex thread after a cold engine restart, preserved the existing report and links, appended the requested synthetic review sentence and saved it successfully.
- A real Assistant request through the installed app read today’s Outlook calendar with its local typed tool, saved a source snapshot, returned to Galaxy and correctly flagged the Council sign-in/loading warning. It did not invent a minister meeting or create/change a report.
- Cold restart: force-stopped the idle Termux runtime and Galaxy app, then launched Galaxy. Its fixed Termux intent restarted the engine; the native home showed **Ready when you are — Running on this tablet · No computer needed**. Saved pairing/history survived. The Linux Galaxy server was stopped, Tailscale was stopped on the tablet, and no USB reverse mapping existed during this test.
- Temporary investigation-only network ADB was restored to USB mode: `service.adb.tcp.port=0`; listener 48441 was gone. The remaining tablet app listeners are 127.0.0.1:4318 and loopback 48442. The temporary host Tailscale Serve route was removed.

Current automated checks: strict TypeScript/Vite production build passes; **52 Node integration tests pass**, including the Python reader suite (now eight tests), on-device identity and isolated document tools/read-only style enforcement. Android APK assembly, **seven** launcher/transfer-policy tests and lint pass. The two helper permission lint suppressions are narrow, documented local-pilot requirements, not a lint bypass. The final parent governance preflight passed 246 tests, one skip and two subtests; required documents, compile checks and the secret scan passed.

Fixes found during the sweep included Android's `process.execPath` resolving to its linker (use the explicit Termux Node binary for the linked Microsoft CLI), local PDF temporary-directory/font setup, helper listener lifecycle handling, and document-tool write refusal for Quick answers/briefings. Termux and the helper were exempted from battery optimization; repeated device calls and cold engine restart passed after setup.

Private evidence is under `.local/verification/2026-09-22/standalone/`, including `model-app-result.json`, `device-validation.log`, `outlook-calendar.json`, `outlook-search.json`, `bridge-policy.log`, `cold-launch.png`, and build/test logs. These artifacts, credentials, real source material and APKs are ignored and must not be committed.

Still requiring field testing: spoken dictation accuracy, full-day battery use, network switching, lock/unlock during a long turn, DeX/stylus use, real cloud uploads and conflicts, and renewed/current access to the Council Outlook account. Existing history and reports were retained during migration, but old internal Codex thread IDs were reset; new threads retain ordinary resume behavior. No offline AI or full terminal surface is claimed.

## Historical September 21 verification

The records below preserve earlier implementation evidence. Their host PIDs, USB prerequisites, account setup states, test counts and pending-work lists describe those earlier stages, not the current standalone deployment above.

## Result

The local pilot now supports gathering documents and emails into an existing workspace, discussing the sources, drafting a report beside chat, direct text refinement, and Word/PDF/Markdown exports. The final Android 0.2.0 build is installed and paired. Real Codex and physical-tablet flows passed with synthetic sources. Existing account identities, cache bytes and host settings were preserved. Actual Microsoft publication remains unverified; cloud UI and protocol tests use fixtures. The latest report-workflow section below is the current closeout; earlier sections retain historical evidence.

## Automated checks

- `./galaxy build`: TypeScript strict checking and Vite production build pass.
- `./galaxy test`: **21 tests passed**. Covers path traversal, hidden/credential filenames, symlink boundaries, current-file snapshots, local conflict refusal, reviewed application, deletion refusal, persistent history, restart recovery, event cursors, common credential redaction, submission idempotency, thread resume, worker/ordinary-child termination, HTTP authentication/origin/Host enforcement, account-specific tokens, cloud conflicts, conditional upload and transfer headers, original-account binding, cross-account imports and source indexes, DOCX/PDF text extraction, and client-ID validation. Six additional tests cover reuse of CLI identity, changed active account, a switch between credential commands, suppression of credential-bearing subprocess errors, drive pinning/write refusal/unlink, and authenticated HTTP import/preview with retained provenance and no bearer on the transfer host.
- Parent `bash scripts/governance-preflight.sh`: **246 passed, 1 skipped, 2 subtests passed**, required documents/compile/secret scan passed. No open governance exceptions.
- npm installation audit: **0 vulnerabilities** at build time.
- New-project credential-pattern scan: zero matches. Runtime state is Git-ignored.
- Existing parent README and portfolio-status changes were preserved. No commit or push was created.

## Real Codex checks

Used the existing ChatGPT login and the installed pinned app-server version. Inputs were the bundled synthetic meeting documents, not the user's OneDrive data.

1. Browser sent the folder assessment through the authenticated API.
2. Codex listed the folder and read Budget.csv, Meeting notes.md, and Project overview.md. All four observed command executions exited 0.
3. The answer referenced the fixture facts and identified inconsistencies.
4. Page reload recovered the saved conversation. A follow-up resumed the same Codex thread and completed.
5. The UI created a separate draft. Codex changed only the requested Catering owner in Budget.csv from Unassigned to Adam; source content remained unchanged.
6. Browser networking was disabled briefly while the draft request ran, then restored. The resulting message existed exactly once and the edit was retained.
7. The conversation URL restored the draft context after reload. The review panel displayed the diff.

Assessment/follow-up check: **59.8 seconds**. Draft/reconnect check: **27.6 seconds**. Both checks had external wall-clock limits and saved progress. All Codex workers from these checks exited.

Local evidence (ignored, because future runs can contain private data):

- `.local/verification/live-flow.json`
- `.local/verification/live-draft.json`
- `.local/verification/live-conversation.png`
- `.local/verification/draft-review.png`

## Browser checks

Agent-browser opened and inspected the service. Its evaluation helper did not reliably operate in the actual page context in this environment, so the full interactions used the same Chrome instance through direct CDP in a separate test tab.

- 1280 × 800 landscape: no horizontal page overflow.
- 800 × 1280 portrait: no horizontal page overflow.
- 390 × 844 compact layout: no horizontal page overflow.
- Document preview showed the original, unedited sample content.
- “Use in conversation” attached the file to the composer.
- Three separately labeled OneDrive slots and unconfigured setup state rendered.
- No JavaScript exceptions during the browser verification.
- Visual inspection led to larger reading sizes and a more compact landscape welcome layout.
- React review: semantic button labels, native dialog focus/Escape handling, event-stream cleanup, stale-fetch cancellation, reduced-motion support and responsive navigation are present. This is not a formal accessibility certification.

Screenshots and browser report are in `.local/verification/` (`tablet-landscape.png`, `tablet-portrait.png`, `phone-layout.png`, `connections.png`, `browser-flow.json`).

## Physical tablet check

The Galaxy Tab S9 Plus (SM-X810, Android 16) connected after the user enabled Developer options and USB debugging. ADB authorization succeeded. A device-specific `tcp:4318` reverse mapping opened the workstation service in tablet Chrome at `http://localhost:4318`.

- Paired the tablet browser through the pairing form without exposing the pairing credential in logs or command arguments.
- Actual browser viewport: 1317 × 650 CSS pixels; no horizontal page overflow.
- Workspace, composer, sample files, and saved conversation entry rendered. Opened and closed the Budget.csv preview successfully.
- No JavaScript exceptions were observed during pairing and inspection; no error overlay appeared.
- Evidence: `.local/verification/physical-tablet-check.json` and `.local/verification/physical-tablet-workspace.png` (ignored).
- No new model request was submitted during this device check. Physical keyboard, rotation, DeX and lock/reconnect checks remain outstanding.
- The browser is left open and paired, with the USB reverse mapping active for the user to try the sample workspace. This connection requires the USB cable and the running workstation service.

## Outstanding gates

The Android build adds native launcher/connection screens around the embedded workspace. Its APK, four Android tests, lint, tablet pairing/preview/live response, keyboard, rotation, shortcut and cold-restart checks are recorded in [Android app verification](2026-09-22%20-%20Android%20App.md). The host tests also cover the idle SSE opening-frame regression found during the tablet check. The account-reuse change required no Android source or APK changes.

1. Complete extended hands-on DeX, stylus, multi-window and screen-lock/reconnect checks. USB, app touch navigation, keyboard visibility, portrait rotation, pairing, preview and cold restart passed the Android check.
2. Register/configure a Microsoft app, then sign in to all three actual accounts. Work-account policies may require admin involvement.
3. Exercise real cloud imports, mixed-account imports, token renewal/revocation and a deliberately conflicting text save. Guided AI Labs live browsing passed; import and save checks still use fixtures. No live save claim is made; cloud writes are disabled for the reused host connection.
4. Configure a private HTTPS route for daily use away from USB, preserving any existing Tailscale services.
5. Before broader deployment: stronger process/read isolation, a vault-backed token store, retention/backup controls, and a production-supported worker transport.
6. Format-preserving Office editing, full spreadsheets/OCR, a terminal/VS Code fallback, and Windows/Mac hosting remain later increments.

## Session closeout

A lightweight local web service is intentionally left running for the device check:

- PID: `3504047` (restarted after the existing-account integration; the prior PID `3271693` exited after an idle-state check)
- Command: `node server/main.ts` (started through `./galaxy start`)
- Directory: this project folder
- Listener: `127.0.0.1:4318`; browser origin `http://localhost:4318`
- Stop: Ctrl+C in its terminal or SIGTERM after checking that PID still belongs to this service.
- No active Codex turns, heavy jobs, or worker children remained at closeout. The server launches a bounded worker only after an authenticated message.
- The initial build closeout had no connected device; the subsequent physical-device check above established USB forwarding. The user enabled debugging on the tablet. No Tailscale Serve/Funnel, firewall settings, global Codex configuration, or Microsoft tenant settings were changed.

## Existing-account live verification

- Microsoft 365 CLI 11.8.0: active connection and Graph profile verified as `adamgoodwin@guidedailabs.com`; OneDrive ID stored in ignored settings. No new sign-in, app registration, permission grant, or CLI account switch was performed.
- Live root browse returned eight items, including six folders. No private document was downloaded, imported or sent to the AI during setup.
- Agent-browser launched the desktop session; direct CDP drove the existing browser. 1280 × 800 landscape and 800 × 1280 portrait had no horizontal overflow or observed JavaScript exceptions.
- Installed Android app verified through its debug WebView: 1317 × 684 CSS viewport, existing pairing and conversations retained, correct four-card account display, `1/4` connection count, additional-account form collapsed, and live file list loaded. The script checked for unsent composer text before refreshing.
- React/UI review: stable account keys, explicit read-only host label, accurate dynamic count, native expandable setup, and disabled cloud-save button for host imports. Server independently refuses writes.
- Live imports, account revocation, and renewal are not claimed. Automated fixtures verify the import/API path and rejection boundaries. The underlying CLI may hold broader Microsoft permissions; the read-only limit is enforced by Galaxy Workspace.
- Evidence (private/ignored): `.local/verification/existing-connection-browser.json`, `existing-connection-tablet.json`, `existing-connections.png`, and `existing-connections-tablet.png`.
- Build and all 21 tests passed after the final UI change. Changed-code whitespace and credential-pattern checks passed; local settings/database/evidence remain Git-ignored. No heavy child jobs remain.

## Connections button repair

- Reproduced on the installed tablet: the three disconnected accounts showed disabled “Connect OneDrive” buttons while the prerequisite Microsoft setup was collapsed. No application ID is configured for those independent accounts.
- Each unconfigured account now shows “Setup needed” and an enabled “Set up OneDrive” button. Tapping opens the shared setup panel, scrolls it into view, and focuses its heading. Native details opens immediately to handle a quick collapse/reopen before its queued toggle event.
- Added inline setup/account errors, immediate sign-in progress and pending-code feedback. Browse focuses and brings its file list into view; changing folders clears the previous pagination cursor.
- Actual tablet touch checks passed for all three setup buttons and Guided AI Labs live browsing (eight items). Microsoft Entra's external link launched Android Chrome; Galaxy Workspace was then brought back to the foreground. No sign-in, tenant registration, account switch, or consent was completed.
- Desktop browser checks passed at 1280 × 800 and 800 × 1280 with no horizontal overflow or observed JavaScript exceptions. Screenshots of setup and browsing were visually reviewed.
- Isolated browser fixtures verified invalid application-ID feedback, successful setup changing the button to “Connect OneDrive,” failed sign-in feedback, code/link display, expired sign-in retry, browsing, disconnect, and host unlink. All API calls in that fixture tab were intercepted; the real linked account was not disconnected. Initial fixture/harness failures were corrected (missing sample project and attempting to serialize a DOM element through CDP); final checks passed.
- Parent governance preflight passed: 246 tests, one skip, two subtests; no exceptions. Final build and all 21 project tests passed. No Android source or APK changes were needed; the running host serves the new web assets and the installed app was refreshed without losing pairing or conversations.
- Evidence (ignored): `.local/verification/check-connection-buttons.mjs`, `connection-buttons-live.json`, `connection-buttons-tablet.json`, `connection-buttons-fixture.json`, and corresponding setup/browse PNGs. The three independent real Microsoft sign-ins remain outstanding.

## End-user sign-in simplification

- Removed the application-ID form and Microsoft Entra registration instructions from Connections. The ready state offers **Sign in with Microsoft**, a numbered one-time-code flow, and Microsoft’s external sign-in page. The unconfigured state is explicit; **Check sign-in availability** reports local feedback or starts sign-in when host provisioning has finished.
- Developer provisioning moved to `./galaxy configure-microsoft APP_ID`; the paired-browser settings mutation route returns 404. The host command preserves linked-account metadata, rejects active/orphaned sign-ins and replacement of an existing registration, and accepts a repeated identical configuration. Reviewed public-client registration payload: `config/microsoft-registration.json`.
- Governance preflight passed: 246 tests passed, one skipped, two subtests passed; documentation, compile, and secret checks passed. Strict TypeScript/Vite build passed. All 22 project tests passed, including host provisioning guards and the removed browser configuration route.
- Real desktop and installed-tablet checks passed: no application-ID field, no registration instructions, all three unavailable-state controls show a visible explanation, no premature sign-in requests, and live Guided AI Labs browsing still returns eight items. No files were imported. No observed JavaScript exceptions or horizontal overflow. Tablet checks used touch events.
- Browser-only fixtures passed readiness changing while the page is open, all three sign-in controls, code/link display, independent pending accounts, failure/retry, expiry, and connected browsing. Fixtures intercept every API call and do not change real Microsoft accounts or host settings. Desktop landscape and portrait layouts checked; screenshots reviewed. Actual independent Microsoft sign-ins remain unverified.
- Existing CLI capability check: app-registration create/read permissions were absent; a bounded read-only application metadata request returned 403 `Authorization_RequestDenied`. No Microsoft registration, consent, directory changes, account switches, or privilege increases were made. A headed Microsoft Entra sign-in page was opened on Linux; owner authentication is the next dependency.
- Service restarted after confirming no active conversations or worker children; pairing, stored conversations, and the linked host account were preserved. New host PID 3638363, session 9283, loopback port 4318. Android APK unchanged.
- Ignored evidence: `.local/verification/check-simple-signin.mjs`, `simple-signin-{live,tablet,fixture}.json`, corresponding PNGs, and `signin-simplification-tests.log`. Earlier “Connections button repair” findings describe the previous UI; this section supersedes its user setup instructions.
- Closeout: all bounded jobs finished; no host worker children remain. Temporary tablet inspection forwarding and verification tabs were removed. USB reverse port 4318 remains for normal use. The headed Linux browser (PID 3619875, local CDP port 46717) is intentionally left on the Microsoft owner sign-in page for the requested user authentication; it is not running an autonomous task. Continue only after the user signs in, then confirm the owner tenant and finish developer registration.

## Microsoft registration completed

- Adam completed owner authentication in the original Linux browser on CDP port 45623 as `adamgdwn@hotmail.com`, in Default Directory (`adamgdwnhotmail.onmicrosoft.com`). A second browser on port 46717 was initially mistaken for that session; the signed-in window was located without requiring another developer login. The previous handoff's browser PID/port pairing was incorrect.
- Created one new **Galaxy Workspace** registration in that owner directory after confirming the app list was empty. Verified the saved Microsoft Graph manifest: `AzureADandPersonalMicrosoftAccount`, public client enabled, requested access-token version 2, exactly delegated `User.Read` and `Files.ReadWrite`, zero secrets/certificates/redirect URIs. No tenant-wide admin consent or unrelated tenant settings were changed. Private identifiers and verification metadata are in `.local/verification/microsoft-registration.json`.
- Ran the host provisioning command using the verified new client ID. The tablet showed all three **Sign in with Microsoft** buttons, with no developer setup banner or application-ID entry. A real Personal sign-in request obtained a Microsoft device code. In the same owner browser, the displayed confirmation code was matched to that request before proceeding.
- Microsoft required a separate Authenticator approval for Personal OneDrive; the number-matching request timed out before approval. No Personal account token was acquired. The incomplete device-code flow was canceled through a graceful host restart after checking for active work; the account returned to disconnected. No approval requests were retried automatically. All three requested account slots remain ready for user sign-in. Guided AI Labs remains linked and usable.
- Final desktop and physical-tablet checks passed: configuration ready, three enabled sign-in buttons, no stale connecting state, no observed JavaScript exceptions or horizontal overflow, and Guided AI Labs root browsing returned eight items. No cloud documents were imported or changed. Screenshots reviewed. Real completion of the three independent sign-ins remains outstanding.
- Governance preflight passed (246 tests, one skipped, two subtests; docs/compile/secret checks passed). Strict TypeScript/Vite build and all 22 project tests passed. No application source or native Android changes were needed this turn. Host PID 3690159, session 5148, loopback port 4318; prior host stopped cleanly.
- Ignored evidence: `activate-microsoft.mjs`, `verify-microsoft-ready.mjs`, `microsoft-configured-{live,tablet}.json`, corresponding screenshots, `microsoft-live-signin.json` (target metadata only; no code/token), and `microsoft-provisioning-checks.log`. Device codes, OAuth tokens, and passwords were not logged or passed as shell arguments. The Authenticator matching number was shown to Adam only as required for that one sign-in.
- Cleanup complete: both temporary Linux browsers closed, temporary ADB inspection forwarding removed, normal USB reverse port 4318 retained. No compute-heavy children remain; the idle host stays available for the tablet.

## Remove and restore account cards

- Adam subsequently completed Personal and a work sign-in. At the start of this change, slots 1 and 2 were connected, slot 3 was still pending, and the existing host connection was linked. The work slot and host card displayed the same email, consistent with the duplicate Adam reported. No real connection was removed automatically.
- Every card now offers **Remove**, including unsuccessful and pending sign-ins. Removed cards stay absent after a restart. **Add OneDrive account** restores a free independent slot with a fresh label and sign-in. Removal clears only that connection's local credentials and current selections; imported documents, provenance, conversations, other account caches, and OneDrive files are preserved. The host card only unlinks its binding.
- Added regressions for persistence, independent cache/identity preservation, saved-work retention, late successful and failed sign-in responses, reused slots, stale device-code callbacks, and stale cache writes. Authenticated HTTP removal/addition and rejection of unpaired callers passed. Existing host-unlink and provenance checks still pass. Host provisioning accepts removed slots without permitting active/orphaned sign-ins.
- Parent governance preflight passed: 246 tests, one skip, two subtests; documentation, compile, and secret checks passed, with no open exceptions. Strict TypeScript/Vite build and all 26 project tests passed.
- Browser fixtures passed on desktop and the physical tablet using mouse/touch input: removal errors and retry; pending account removal; connected and host duplicate removal; clearing only the selected account's file selections; closing its browser; restoring an empty sign-in card; and recovering from an entirely empty connection list. Fixture fetches were intercepted, with zero live removal/add POSTs. The first fixture lacked the required sample project and was corrected before these successful runs.
- Live desktop and tablet checks confirmed four usable Remove buttons, three connected entries, no observed JavaScript exceptions, and no horizontal overflow. Screenshots reviewed. All real signed-in identities, existing cache-file hashes, and host settings match the pre-update state. The unfinished slot-3 attempt was canceled by the idle host restart and is now disconnected. No private cloud document content was opened or changed during verification.
- No Android source/APK changes were needed. The installed app was reloaded to use the new host assets. Host PID 4092758, session 66544, remains available on loopback port 4318; no active conversations. Temporary browser and tablet inspection forwarding were closed; normal USB reverse port 4318 remains.
- Ignored evidence: `.local/verification/remove-connections-{preflight,build,tests}.log`, `check-remove-connections.mjs`, `remove-connections-{desktop,tablet}-{live,fixture}.json`, corresponding PNGs, and `remove-connections-preserved.json`. Earlier account-setup status is superseded by this section.


## Gather, discuss, draft and export workflow

- Added new workspaces, additive material batches, upload and pasted-email forms, OneDrive import into the current workspace/conversation, persistent report selection, automatic draft preparation, report rendering beside chat, direct Markdown editing, and export controls. Workspaces resume their latest saved conversation; reload restores its correct workspace and Report tab. Relative citations resolve against the report’s own directory and open source previews. Markdown tables render as tables with contained horizontal scrolling on small screens.
- File input supports DOCX, PDF, text/Markdown, CSV/TSV, JSON and `.eml`. MIME headers/body and supported email attachments are decoded; original email bytes stay intact. Python standard-library parsing loads no remote HTML resources. Failed pre-publication batches leave no partial source files. New material checkpoints only its source paths in the current draft; an existing report edit remains unchanged. Other drafts keep their previous snapshots.
- Export snapshots require the viewed hash, produce real DOCX/PDF/Markdown artifacts, and require pairing to download. Tests inspect Word/PDF contents, reject stale edits/exports and ensure embedded local resources are not included. OneDrive tests pin identity and folder, require conflict behavior `fail`, omit the Graph bearer on the transfer, and prevent replay of an uncertain upload. A new workspace’s original source files are not overwritten by drafting.
- Parent governance preflight passed: 246 tests, one skipped, two subtests; docs, compile and secret checks passed. Parent/subproject exceptions remain empty. Final strict TypeScript/Vite build and **33 project tests passed**. `remark-gfm` 4.0.1 was already present on the workstation and added to this project from the local npm cache; the install reported zero vulnerabilities. Final Android assemble, **7 unit tests**, and lint passed (zero errors; existing Android plugin upgrade advisory remains).
- Desktop live flow uploaded the synthetic Word fixture and an `.eml`, then added a pasted email to the same workspace. One real, bounded Codex turn wrote a roughly 330-word report with source citations, dates, owners, budget and explicit unknowns. It did not modify the seven original source files. A direct refinement was saved, and a Word export downloaded. The later native file import added two more baseline files; all **nine source hashes** remained unchanged at closeout. The synthetic **Report workflow demo** workspace and saved report are intentionally retained for Adam.
- On the actual SM-X810, Android’s document picker selected a synthetic text file from Downloads and the WebView uploaded its content URI into the existing conversation draft. The report remained intact. Android’s save dialog wrote Word and PDF exports; both files were pulled back and independently parsed, confirming the report text, budget and manual review note. The final APK also passed a save-dialog check with the readable default filename `Summary report.docx`. No broad storage permission, native JavaScript bridge, system management or device reset was added.
- Desktop and actual-tablet cloud fixtures selected material across folders and accounts, retained the current project/conversation IDs and connection pins, selected an export destination folder, displayed a successful receipt, and disabled resubmission after an unconfirmed result. All cloud mutations were intercepted. Database verification confirms **zero live cloud writes** during this work. Real-account export remains a deliberate follow-up test for the user.
- Final visual checks passed at 1440×1050, 800×1280, 390×844 and actual tablet 1317×684 CSS pixels: no horizontal page overflow, no observed JavaScript exceptions, readable report layout/tables, working source citation previews, and manual editor state retained across Files/Report tab changes. Screenshots were reviewed.
- Verification caught and fixed a file-input state timing bug, a restored-conversation/workspace-name race, and report-relative citation routing. Harness corrections distinguished the worker’s `complete` status from `completed`, forced a document navigation before reload checks, and waited for enabled folder controls. One tablet task was removed while a picker was open; there was no app crash record, and subsequent native picker tests completed. No additional AI request was replayed for harness failures.
- Final runtime: PID **100843**, command `node server/main.ts`, exec session **30822**, loopback `127.0.0.1:4318`, origin `http://localhost:4318`. No active conversations or worker children. APK 0.2.0 SHA-256: `ddb04e386b8e10e1bff86c722c3e818fcc0e406ac4d562ee573d977ac4df7b34`. Host and normal tablet USB reverse mapping stay available. Temporary inspection forwarding, verification browsers, and synthetic files in tablet Downloads are removed at closeout; host verification artifacts and the demonstration workspace remain.
- Private evidence in `.local/verification/`: `report-workflow-{preflight,build,tests,android}.log`, `report-workflow-preserved.json`, protected pre-update SQLite backup, `report-desktop.json`, `report-cloud-ui-{desktop,tablet}.json`, `report-final-{desktop,tablet}.json`, `report-tablet-downloads.json`, parsed-test DOCX/PDF artifacts, final screenshots and verification scripts. No commit or push was made; unrelated parent changes were preserved.

## Meeting shortcuts and dictation readiness

- Governance preflight passed: 246 tests passed, one skipped and two subtests passed. Both project-control files retain `exceptions: []`.
- Strict TypeScript/Vite build and **36/36 application tests** passed. Added coverage verifies read-only thread/turn policies for quick, brief and explanation requests inside a draft, legacy full-conversation behavior, duplicate submission handling, style validation, and rebasing Markdown citations without damaging tables or literal code. Markdown parser packages already present in the dependency graph were made explicit dependencies using the offline cache; no additional packages were downloaded.
- Two real, bounded Codex turns ran against synthetic material: a budget question found the $1,200 cap and identified absent evidence of council approval; the one-tap briefing produced the requested four sections in 192 words. No turn was replayed. Two harness assertions initially ran before the UI caught up; checks resumed against the saved results with explicit state waits.
- Desktop and actual tablet checks verified working answer/report source previews, review-before-save report additions, preservation of existing saved text and unsaved editor text, and automatic Full conversation selection for Draft report. Explain further's one-request payload was verified with an intercepted submission, alongside real worker-policy tests; no extra model turn was needed. Only final answers receive the two follow-up controls.
- Layouts passed at 1440×1050, 800×1280, 390×844, simulated 1317×349, and physical tablet 1317×684. No observed JavaScript exceptions or horizontal page overflow. Native Samsung Keyboard also passed at 1317×349 with the Send control visible. A keyboard check found and fixed composer controls falling below the visible viewport; short-height layouts now compact the headers. Screenshots were reviewed.
- Samsung Keyboard remains the default. Google voice typing was already installed, enabled and granted microphone permission; no speech app was installed. `show_ime_with_hard_keyboard` was changed from 0 to 1 so the microphone can be reached with the physical keyboard attached. The microphone is visible at the bottom left of the open keyboard. Actual speech recognition accuracy was not tested; no microphone capture was initiated for verification.
- Accounts, settings and existing token-cache bytes are preserved. **Zero live cloud writes** and no active workers at closeout. A new conversation in Report workflow demo contains the briefing and a saved report copy; the prior report conversation remains intact. No Android source or APK changes were needed; the existing 0.2.0 shell loads the new host assets.
- Current host: PID **291812**, `node server/main.ts`, exec session **98812**, loopback `127.0.0.1:4318`, origin `http://localhost:4318`. The host and normal USB reverse mapping remain available. Verification browsers, temporary inspection forwarding and temporary tablet UI dumps are removed at closeout.
- Private evidence: `meeting-{preflight,build,tests}.log`, `meeting-live.json`, `meeting-final-{desktop,tablet}.json`, `meeting-keyboard.json`, `meeting-preserved.json`, `meeting-final-processes.json`, protected pre-update database backup and screenshots under `.local/verification/`. No commit or push; unrelated parent changes preserved.

## Permanent tablet System workspace

- Governance preflight passed: 246 tests, one skip and two passing subtests; no open exceptions. Strict TypeScript/Vite build and **43/43 application tests** pass. The installed Codex 0.154.0-alpha.6.2 protocol was generated locally to verify its experimental dynamic-tool interface.
- System is seeded once, sorted first and persists across restarts. Its scope is the linked Galaxy tablet. Codex receives five typed tablet tools, a read-only worker sandbox and disabled shell execution. Normal workspaces retain their document/report workflow. System rejects document-import, draft, report and publication mutations independently of the UI.
- Automated checks cover device/path validation, quoted filenames, private/credential path exclusions, proposal-only tool behavior, supported settings, conditional Apply/Undo, original null/default restoration, duplicate submissions, stale/expired/wrong-device proposals, stopped workers, uncertain writes and restart recovery. Worker and HTTP tests verify pairing/origin requirements, dynamic tool dispatch/resume and worker scope.
- Two actual Codex turns completed against synthetic tablet material. Codex collected the tablet snapshot, found and read the test file, and proposed a ten-minute screen timeout without applying it. Native tablet touch applied the proposal and then undid it; ADB independently confirmed **300000 → 600000 → 300000 ms**. The resumed conversation checked the restored five-minute timeout. Snapshot results now include conversation action receipts so later answers can distinguish proposals from verified changes.
- Live checks identified and fixed Android Toybox's different `find` formatting and ADB `exec-out` quoting behavior. The actual filename search, file preview and temporary-copy cleanup passed after those fixes. No real private document content was used for the model tests.
- Final browser and installed-tablet checks passed shared-file browsing and preview, permanent workspace identity, persisted Undo receipt, System-only controls and the normal workspace controls. An isolated status fixture verified disconnected guidance without changing the real USB connection. Desktop layouts passed at 1440×1050, 800×1280, 390×844 and 1317×349. Actual tablet checks passed at 1317×684 and with Samsung Keyboard open at 1317×349; Send remained visible, no horizontal overflow or JavaScript exceptions were observed. New conversations reset scroll position; setting cards follow the conversation when the user is already following its latest content.
- Accounts were compared directly against the protected pre-update database backup. Existing settings and token-cache bytes are unchanged. No live cloud writes occurred. The screen timeout is restored to five minutes; the previously enabled on-screen keyboard setting remains enabled. No Android source, APK or Android permissions changed in this increment.
- This pilot requires the current Linux host and authorized USB ADB connection. It can inspect shared files, installed user-app identifiers and current health/settings. Changes are limited to screen timeout, automatic brightness, auto-rotate and keyboard visibility, with reviewed Apply and conditional Undo. It does not provide historical battery-drain attribution, private app-data access, rooting, flashing, arbitrary shell commands or autonomous cleanup. Wireless access remains future work.
- Final host: PID **433069**, `node server/main.ts`, exec session **92862**, listening only at `127.0.0.1:4318`, browser origin `http://localhost:4318`. The host and normal tablet USB reverse mapping remain available. All model turns and bounded build/test/browser jobs have finished; no heavy worker remains. The verification browser, temporary ADB inspection forward and synthetic tablet file are removed at closeout. The System demonstration conversation and private host evidence remain available.
- Private evidence under `.local/verification/`: `system-{preflight,protocol,build,tests}.log`, protected `system-before.sqlite`, `system-device-files.json`, `system-tablet-live.json`, `system-final-{desktop,tablet}.json`, `system-keyboard.json`, `system-preserved.json`, `system-final-processes.json`, screenshots and bounded verification scripts. No commit or push; unrelated parent edits preserved.

## Conversational Assistant and Outlook meeting preparation

- Parent governance preflight passed: 246 tests, one skipped, two passing subtests. Both project-control files have no open exceptions. Final strict TypeScript/Vite build and **50/50 application tests** pass; the embedded Python reader checks also pass. Final Android build, seven main-shell origin-policy unit tests, and both lint tasks pass. The reader lint report has no issues.
- Assistant is permanent and first in the workspace list; System remains separate. Each new Assistant conversation has an isolated local meeting folder. Saving the first brief registers a named meeting workspace and keeps the same conversation. Subsequent model and manual edits use the current report hash to prevent overwriting newer text. Added source material works in the saved meeting folder without copying files onto themselves or requiring a Git draft baseline.
- Eight typed tools provide Outlook calendar/search/current-screen reading, fresh-reference message/event opening, bounded scrolling/back navigation, local source reading and brief saving. The Codex worker retains disabled shell execution and a read-only sandbox. Host-side tools bind to the configured serial/model, reject concurrent/late/stopped operations, save dated source snapshots, and return the tablet to Galaxy after a completed/stopped read. No arbitrary taps, sending, invitation response or account changes are exposed to the worker.
- Installed the separate **Galaxy Outlook Reader 0.1.0** helper on the linked SM-X810. The existing main shell remains installed at 0.2.0. The helper is a transient ADB instrumentation reader with no launcher, background service, network/account/storage/input permissions, or device-side capture files. Main-app pairing and data were preserved.
- Real-device investigation found three independent Outlook issues: standard UI dumps can fail waiting for global idle; Samsung keyboard Enter can leave a suggested search unsubmitted; and this Outlook version's Mail-only results expose blank accessibility rows. The helper avoids the global idle wait; the host submits only the exact search suggestion and uses **All** results, which expose readable message cards. Message expansion and conversation-body scrolling were also verified. Failed/stale standard dumps are never used as production source captures. The reader remains bounded to the visible interface and does not guarantee full mailbox or body coverage.
- **Two real Codex preparation turns completed.** The first saved an explicit outline despite inaccessible current Council data. After the search repair, the resumed turn read the calendar, searched, opened and expanded a recent Council message, scrolled and searched further, then refined the same brief to about 445 words. It used seven of the requested eight Outlook operations. All second-pass tool calls completed without errors. The examined MCMC invitation was correctly identified as September 17, not the requested September 22 meeting. No substitute meeting or minister identity was invented.
- Outlook requested sign-in for the Council account and the calendar displayed a fetching warning. The saved brief prominently records incomplete coverage and unverified current meeting details. The UI retains account warnings throughout a reading pass even when Outlook's snackbar disappears; transient collapsed-message warnings clear after expansion. A later reading pass can replace the prior warning state. Owner sign-in in Outlook remains necessary to verify current Council access.
- The named Council preparation workspace and `Reports/Meeting brief.md` are retained for Adam. All **11 source snapshots** remained unchanged through manual editing and export checks. Every local source citation resolves; report-relative links work from the nested Reports folder. Direct editing survives Files/Meeting brief tab switches. Real Word and PDF exports were downloaded through the paired browser and independently parsed, including the added meeting-notes text. These files are local copies; **zero live OneDrive writes** occurred.
- Desktop checks passed at 1440×1050, 800×1280, 390×844 and 1317×349. Physical-tablet touch checks passed at 1317×684 and with Samsung Keyboard open at 1317×349: source preview, direct edit/cancel, fresh Assistant entry and visible Send control. No observed JavaScript exceptions or horizontal page overflow. Screenshots were visually reviewed. Voice typing's microphone remains visible; speech-recognition accuracy was not tested.
- Verification harness corrections: one resume poll observed the previous completed state before the newly submitted turn started. The running turn was followed to completion without resubmission. Report-link checks distinguish file citations from automatically rendered email links. A UI warning assertion revealed the disappearing-snackbar problem, which was fixed and regression tested.
- Accounts match the protected pre-update SQLite backup. Existing settings and Microsoft token-cache bytes are unchanged. The tablet screen timeout remains 300000 ms and its previously enabled on-screen-keyboard setting remains 1. This increment did not send email, change events, sign into accounts, request Microsoft grants, or alter Codex/Claude global configuration.
- Final host: PID **625894**, `node server/main.ts`, exec session **57915**, loopback `127.0.0.1:4318`, origin `http://localhost:4318`. The host and normal USB reverse mapping stay available. All model/build/test jobs have finished. Temporary verification browsers, inspection forwarding and device XML are removed at closeout; no compute-heavy child remains. Assistant is left ready on the tablet. Unrelated parent changes were preserved; no commit or push was made.
- Private ignored evidence: `outlook-ui-preflight.log`, protected `outlook-before.sqlite`, `assistant-{live,resume,final-desktop,final-tablet,keyboard,preserved}.json`, `assistant-final-{build,tests,android}.log`, `assistant-reader-tests.log`, `assistant-open-{mail,full}.json`, `assistant-search-all.json`, parsed `assistant-brief.docx`/`.pdf`, corresponding screenshots and bounded verification scripts under `.local/verification/`.
- Final long-message follow-up found and fixed two navigation details: the driver must retain an already foregrounded full-message activity, and scroll within the message-detail container rather than using a WebView child's unclipped rectangle. A fresh real-device expansion → scroll → read → back check passed: body text advanced, the repeated read stayed on that body, and Back returned to the conversation. Python regressions cover keeping the foreground activity and selecting the bounded message scroll area. This was a direct reader check; no third model turn was submitted. Evidence: `assistant-long-message-check.json` and private `assistant-long-message.json`.

## Standalone closeout

All owned builds, verification batches and model workers completed; the tablet’s local engine and authenticated Accessibility helper intentionally remain available for use. No background model turn is running. The earlier Linux Galaxy server is stopped. Documentation and the entire previously untracked Galaxy subproject are included in the release commit; unrelated parent README/portfolio changes are excluded. Runtime credentials, saved work, private evidence and build outputs are excluded. The commit and remote reference are reported in the user handoff.
