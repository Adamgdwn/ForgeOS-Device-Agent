# Optional, non-secret local settings. The app does not load .env files automatically.
# Default: loopback only. Use a private HTTPS proxy for the tablet.
GALAXY_PORT=4318
GALAXY_ORIGIN=http://localhost:4318
# Microsoft public-client app registration supporting personal + organizational accounts.
# Configure in the app's Connections screen after registration. No client secret required.
