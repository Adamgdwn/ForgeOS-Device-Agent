# The broker starts idle. A local Forge app request is required for takeover.
service forge_sessiond /system/bin/forge-sessiond /data/local/tmp/forge-native.mdJ5BP
    class late_start
    user root
    group root inet
    seclabel u:r:su:s0
    disabled

on property:sys.boot_completed=1
    start forge_sessiond
